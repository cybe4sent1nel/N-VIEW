# N-VIEW Docs

This documentation describes the N-VIEW scanning workflow, configuration, and reporting model.

- Use interactive menu mode for guided scans.
- Use direct scan mode for automation.
- Configure AI via `.env` or the `configure-ai` command.
- Run `bootstrap` for production setup (dependency checks + command launchers).
- Run `doctor` anytime to verify runtime health.
