# Feature Summary

- Numbered menu UX
- Natural-language scan normalization
- Preset scan profiles
- OS and distro detection for diagnostics
- Automatic dependency checks for runtime readiness
- Automatic Nmap installation attempts on supported package managers
- Manual install fallback guidance when automation fails
- Bootstrap command for one-step production preparation
- Cross-platform launcher creation for `nview` and `n-view`
- AI report generation with provider routing
- Local output parsing and structured summaries
